integrate into /etc/bash.bashrc for all users on the system
HISTTIMEFORMAT="%F %T "

alias tl="telnet localhost"
alias nt="sudo netstat -tulpn"

alias ..="cd .."
alias ..2="cd ../.."
alias ..3="cd ../../.."

alias cdo="cd /etc/openvpn/"
alias cdlo="cd /var/log/openvpn/"

alias sysupd="sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y"


